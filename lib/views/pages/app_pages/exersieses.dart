import 'package:flutter/material.dart';

class Exersieses extends StatelessWidget {
  const Exersieses({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: Colors.black,
    );
  }
}