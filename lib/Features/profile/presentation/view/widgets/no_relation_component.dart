import 'package:flutter/material.dart';

class NoRelationComponent extends StatelessWidget {
  const NoRelationComponent({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
